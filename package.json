{
  "name": "road-dj",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev:worker": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_KEY": {
        "description": "Choose a long private password used only to open Road DJ's owner Spotify setup. Guests never need or see it."
      }
    }
  }
}
